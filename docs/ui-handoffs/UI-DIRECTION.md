# UI-DIRECTION handoff

## Provenance

- Workspace: `/Users/racosta/conductor/workspaces/firma23-ops/ui-direction`
- Branch: `firma23-ui-direction`
- Exact accepted UI-FOUNDATION base:
  `27deb897b0cc39979438d225690d72fb50d5b144`
- Foundation bootstrap merge base:
  `dc54699b9c9be84504af237562cca6c12b1626ef`
- Worktree was clean and exactly at the accepted Foundation SHA before editing.
- Source: Luis-approved FIRMA23 UI Direction Control Packet.

## Ownership

This documentation lane edited only:

- `docs/UI-DIRECTION.md`
- `docs/UI-REFERENCE-CATALOG.md`
- `docs/ui-handoffs/UI-DIRECTION.md`

No code, Foundation implementation, tokens, packages, copy, data, types, Auth,
Supabase, Vercel or deployment configuration changed. No reference component was
downloaded or installed.

## Reconciliation

Read-only reconciliation against accepted Foundation found no conflict:

- The paper-white field and black structural rail fit Authority and Systems
  modes.
- The exact 92px compact/292px transient sidebar model remains authoritative.
- The mobile route bar, command palette focus model and 44px controls satisfy the
  direction's interaction requirements.
- `MeshDriftCanvas` remains the sole gradient/grain exception and stays on Home.
- Existing `OperationalHeader`, `FinanceMetricCard`, Revenue Rail, Operator Card
  and state family remain the starting components; the direction does not
  authorize replacing them wholesale.
- No shell rewrite or token expansion is requested.

## Durable decisions

- Three visual modes plus one Home signature are now explicit.
- All 30 supplied references have a V1/later/reject classification.
- V1 route placement and shared-pattern ownership are fixed.
- V1 dependency additions are prohibited.
- Money, AI, telemetry and chart truth rules are explicit.
- Final browser acceptance belongs only to the integrated exact SHA.

## Route-lane requirements

Each route lane must read `docs/UI-DIRECTION.md`,
`docs/UI-REFERENCE-CATALOG.md`, `docs/UI-WORKSPACE-LAUNCH-PLAN.md` and its
Foundation handoff before editing. References are design inputs, not component
install instructions. Route lanes consume frozen shared components and record
shared requests in their tracked handoff.

Two inherited route defects are already assigned:

- `UI-OPORTUNIDADES` adds exactly one heading to the member-denied opportunity
  detail presentation.
- `UI-ADMIN` adds exactly one heading to the member-denied settlement detail
  presentation.

Neither lane should globally change `PermissionDenied` because denied list routes
already have their own page heading.

## Luis decisions

No decision blocks V1. Defaults remain:

- monochrome Player Mode;
- private personal spend;
- founders excluded from competition they adjudicate.

## Verification

- `git diff --check` is the required verification for this documentation-only
  lane.
- No browser, unit, build, remote or deployment claim is made by this lane.
- UI-FOUNDATION browser acceptance remains recorded against its exact base SHA;
  the Integrator must rerun the complete matrix after route integration.

## Remote actions

None. This lane is local-only: no push, PR, merge, Preview, Production, Supabase,
Vercel, credential or external-service write is authorized or performed.
